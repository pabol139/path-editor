export interface PointType {
  id: string;
  id_command: string;
  coordinate_index: number;
  radius: string;
  cx: string;
  cy: string;
  control: boolean;
  hovered: boolean;
  selected: boolean;
}
