export interface CircleType {
  id: string;
  id_command: string;
  coordinate_index: number;
  radius: string;
  cy: string;
  cx: string;
  control: boolean;
  strokeWidth: string;
  fill: string;
  hovered: boolean;
  selected: boolean;
}
